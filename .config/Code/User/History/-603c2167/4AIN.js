const a = new Array('name', 'roll');

document.querySelector('.demo').innerHTML = a

a.push('helo')

for (let innerHTML = 0; innerHTML < array.length; innerHTML++) {
    const element = array[innerHTML];
    
}