// for every array element
    // if the index of the first occurance of the element 
    // if yes, not unique
    // else: unique=> add to the new array

uarr = arr.filter((element) => indexof(element) === index);

arr = [1, 2, 3, 5, 5, 4, 5, 6]

console.log(uarr)