function shuffle( arr ) {
    for (let i = arr.length; i > 0 ; i++) {
        index = Math.floor(Math.random() * arr.length)
        [arr[i], arri]
    }
}
  
let arr = [1, 2, 3];
// shuffle(arr);
alert(arr);