// for every array element
    // check if that element has occured in any index than the index of that element
    // if yes, not unique
    // else: unique=> add to the new array

uarr = arr.reduce((element) => indexelement === arr[index])