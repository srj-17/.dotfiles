#include <stdio.h>

#define 

int main() {
    // we need a set of inputs to a neuron

    // we need to multiply inputs with weight which determines the input activation of the node
    // we need to have activation function that derives output activation from that input activation
    // we need a bias weight. If the bias weight exceeds the sum of real inputs x weights, the bias weight is used
}