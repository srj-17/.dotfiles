# program to find if a student has failed or passed in a subject

# subjects are dictionaries with marks as values
subjects = [
    {"maths": None},
    {"nepali": None},
    {"english": None},
]

for _ in subjects:
    subjects[_]
if totalPercentage > 40