#ifndef EXTERNAL_H 
#define EXTERNAL_H // for uniquely identifying this header file. This name can be anything

void display();

#endif

// ifndef is used here because we don't want to include multiple header files in the same program