Make the digits size decrease with display

We do this by making the digits and display both
take the units of viewport width(vw), because it 
means that when the width of device decreases, 
both of the display and the font inside the display
decrease in size.
We can use any relative sizing here, but we MUST
use the 
# Please enter the commit message for your changes. Lines starting
# with '#' will be ignored, and an empty message aborts the commit.
#
# On branch displaysize
# Changes to be committed:
#	modified:   style.css
#
