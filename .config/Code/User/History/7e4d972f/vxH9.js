// do this, for readability
let message = 'Hello world!';
const AGE = 25;
// only capitalcase for the hard-coded constants
// not for constants passed from function like const a = sth(age)
alert(message);

// or this
let m = 'helo', 
    t = 25;

/* or this
let m = 'hello'
  , age = 25;
*/

alert (m + t);
// not this


var me = 'elo', thus = 25;

alert(me + thus);

// declare acc to personal taste

// older scripts use 'var' instead of 'let'