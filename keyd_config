[ids]

*

[main]

# Maps capslock to escape when pressed 
# (not overloaded because I want it to work fast)
# and overloading causes 150ms delay, it seems
capslock = esc

# Remaps the escape key to capslock
esc = capslock
