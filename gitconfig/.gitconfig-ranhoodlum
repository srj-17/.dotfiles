[user]
	name = ranhoodlum
	email = ranhoodlum17@gmail.com

[core]
	sshCommand = ssh -i ~/.ssh/ranhoodlum

[push]
    # push submodules before the parent repo to avoid 
    # leaving the repo on a broken state
    recurseSubmodules = on-demand 
